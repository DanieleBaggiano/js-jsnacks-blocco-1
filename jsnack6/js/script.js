// JSnack 6
// Stampa il cubo dei primi N numeri,
// dove N è un numero indicato dall'utente.

const userNum = parseInt(prompt("dimmi un numero"));

for (let i = 1; i <= userNum; i++);
const cubo = userNum ** 3;
console.log(`Il cubo di ${userNum} è ${cubo}`);